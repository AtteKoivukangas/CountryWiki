import React from 'react';

const LoadingAlert = () => (
  <div>
    <h2>Loading...</h2>
    <p>Please wait a while. This shouldn't take more than couple of seconds</p>
  </div>
);

export default LoadingAlert;